#!/usr/bin/node
// Class Rectangle that defines rectangle

module.exports = class Rectangle {
  constructor (width, height) {
    if (width > 0 && height > 0) {
      this.width = width;
      this.height = height;
    }
  }
};
